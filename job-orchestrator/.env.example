# Job Orchestrator bootstrap environment.
# DATABASE_URL and SHARED_REDIS_URL are intentionally absent: connectors load
# capability-scoped records directly from Vault.
VAULT_ADDR=http://vault:8200
VAULT_TOKEN=
# Production: use VAULT_KUBERNETES_ROLE or AppRole with the JO policy.
